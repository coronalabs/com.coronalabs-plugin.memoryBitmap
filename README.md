Sources for the plugin `plugin.memoryBitmap`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.memoryBitmap" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
